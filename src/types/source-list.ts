export type SourceEntry = {
  id: string;
  name: string;
  description: string;
  version: string;
  icon_url: string;
  source_url: string;
  download_url: string;
  tags: string[];
  language: string;
  content_rating: string;
};
